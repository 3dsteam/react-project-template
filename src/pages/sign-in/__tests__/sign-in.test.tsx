it.todo("renders the sign-in page");

describe("Elements render", () => {
    it.todo("renders the logo");

    it.todo("renders the sign-in form");
});

describe("When the sign-in form is submitted", () => {
    it.todo("sends a request to the server");

    describe("When the request is successful", () => {
        it.todo("calls the Redux authentication action");
    });

    describe("When the request is unsuccessful", () => {
        it.todo("displays an error message");
    });
});
